# ESP32-P4 KeyLogger — C6 firmware WebFlasher

Flash the C6 (Wi-Fi web UI) side of the ESP32-P4 hub keylogger.

This writes the ESP32-C6 firmware — Wi-Fi AP, web UI, ported BadUSB dashboard — onto the on-board C6 module. Flash the P4 first; this is the SECOND step. Move the U4 switch to position 1 before starting.

This is a static web project generated by
[FlasherForge](https://github.com/QWavey). It flashes firmware to the device
**directly from the browser** over ESP32 (WebSerial + esptool-js) — no toolchain, no
command line. Everything runs locally in the page; nothing is uploaded.

## Run it

It is a static site. Serve the folder over HTTP (WebSerial/WebUSB need a secure
context — `http://localhost` counts) and open it in Chrome or Edge:

```bash
cd esp32-p4-hub-keylogger-c6
python -m http.server 8000
# then open http://localhost:8000
```

Or publish the folder to GitHub Pages / any static host over HTTPS.

## Browser support

Needs **WebSerial** — Chrome or Edge (desktop). Firefox and Safari do not implement it.

## Files

```
LICENSE
assets/favicon.svg
css/style.css
firmware/app.bin
firmware/bootloader.bin
firmware/manifest.json
firmware/partition-table.bin
firmware/storage.bin
flasherforge.project.json
index.html
js/flasher.js
vendor/esptool-bundle.js
```

## Open source

The firmware images and this flasher's source are provided. Build the firmware yourself or grab the raw images from the repository.

## License

MIT — © 2026 QWavey. See `LICENSE`.

---

Generated by FlasherForge · figure traced with photo_to_figure.
